
<img width="2548" height="1397" alt="image_2025-11-22_13-27-19" src="https://github.com/user-attachments/assets/9ce5a97a-dac5-470a-9577-e44b8269f8ce" />
<img width="2559" height="1399" alt="image_2025-11-22_13-29-49" src="https://github.com/user-attachments/assets/0f3cb797-12c2-4e92-9985-91fc264fcc46" />


# StarPets Marketplace · Nuxt 4 + FSD

Маркетплейс для покупки игровых предметов StarPets. Проект построен на Nuxt 4 (SSR), TypeScript, Pinia и TanStack Query с архитектурой по принципам Feature-Sliced Design (FSD). Стилизация выполнена на SCSS.

## 🌐 Live Demo

Приложение развернуто на Vercel и доступно по адресу:

🔗 **[https://sp-test-task-eosin.vercel.app/](https://sp-test-task-eosin.vercel.app/)**

Приложение работает с полной поддержкой SSR (Server-Side Rendering).

## 🚀 Основные возможности

### Каталог товаров
- **SSR (Server-Side Rendering)** — товары загружаются на сервере для быстрой первоначальной отрисовки
- **Infinite Scroll** — автоматическая подгрузка товаров при прокрутке до конца списка
- **Skeleton Loaders** — индикаторы загрузки для улучшения UX
- **Адаптивный дизайн** — полная поддержка мобильных устройств и планшетов

### Фильтрация и поиск
- **Фильтры по типам предметов**: Транспорт, Питомцы, Яйца, Зелья
- **Фильтры по редкости**: Common, Uncommon, Rare, Ultra Rare, Legendary
- **Фильтры по уровням** (для питомцев): Обычный, Неон, Мега-неон
- **Фильтры по свойствам** (для питомцев): Флай, Райд
- **Фильтр по цене**: Диапазон от/до с валидацией
- **Поиск по названию**: Debounce 500ms для оптимизации запросов
- **Сортировка**: По популярности, выгоде, цене (возрастание/убывание)

### Live Feed (Лента покупок)
- Отображение последних покупок в реальном времени
- Статистическая карточка с общим количеством сделок
- Горизонтальная прокрутка для удобного просмотра
- Анимированные графики для визуализации данных

### Корзина
- Добавление/удаление товаров
- Изменение количества товаров
- Проверка баланса перед покупкой
- Автоматическое закрытие после успешной покупки
- Подсчет общей стоимости
- Отображение количества предметов

### Хедер
- Адаптивный логотип (меняется на мобильных устройствах)
- Отображение баланса пользователя
- Кнопка корзины с индикатором количества товаров
- Выпадающее меню корзины

### UI/UX
- Glass-panel эффекты для карточек
- Плавные анимации и переходы
- Кастомные скроллбары
- Оптимизированные изображения через Nuxt Image
- Поддержка темной темы (через CSS переменные)

## 🛠 Технологический стек

- **Framework**: Nuxt 4.1.3
- **Language**: TypeScript 5.8.3
- **State Management**: Pinia 3.0.3
- **Data Fetching**: TanStack Query 5.91.2
- **Styling**: SCSS
- **Build Tool**: Vite 7.1.9
- **Image Optimization**: Nuxt Image 2.0.0
- **Composables**: VueUse 14.0.0

## 📋 Требования

- Node.js 18+
- npm или yarn

## 🔧 Установка

```bash
# Установка зависимостей
npm install
```

## 🎯 Скрипты

| Скрипт         | Назначение                                  |
| -------------- | ------------------------------------------- |
| `npm run dev`  | Дев-сервер `http://localhost:3000` (SSR)    |
| `npm run build`| Продакшн-сборка Nuxt                        |
| `npm run start`| Запуск собранного приложения (Nitro server) |
| `npm run lint` | Проверка ESLint                             |
| `npm test`     | Запуск тестов (Vitest)                      |

## 🐳 Docker

Проект поддерживает Docker для удобного развертывания:

```bash
# Сборка и запуск через Docker Compose
docker-compose up --build

# Приложение будет доступно на http://localhost:3000
```

## ⚙️ Конфигурация

### Переменные окружения

По умолчанию используется публичный API `https://market.apineural.com/api/store`. 

Для изменения базового URL создайте файл `.env` в корне проекта:

```bash
# Скопируйте пример файла
cp .env.example .env
```

Или создайте `.env` вручную:

```env
MARKET_API_BASE=https://market.apineural.com/api/store
```

Для изменения URL отредактируйте значение `MARKET_API_BASE` в файле `.env`.

## 📁 Архитектура

Проект организован по принципам **Feature-Sliced Design (FSD)**:

```
app/
├── pages/          # Страницы приложения
├── layouts/        # Макеты страниц
└── assets/         # Глобальные стили и ресурсы

entities/           # Бизнес-сущности (Item, User)
features/           # Функциональные возможности (Cart, Filters, LiveFeed)
widgets/            # Составные компоненты (Header, Catalog)
shared/             # Переиспользуемый код (API, утилиты, конфиги)
```

## 🎨 Особенности реализации

- **SSR для товаров**: Первая страница товаров загружается на сервере для улучшения SEO и скорости загрузки
- **Оптимизация запросов**: Debounce для поиска, стабилизация payload для предотвращения дублирования запросов
- **Lazy Loading**: Асинхронная загрузка компонентов фильтров для улучшения производительности
- **Type Safety**: Полная типизация TypeScript для всех компонентов и API
- **Error Handling**: Обработка ошибок на всех уровнях приложения
- **Accessibility**: Поддержка ARIA-атрибутов и семантической разметки

## 📝 Дополнительно

- Для форматирования валюты используется `Intl.NumberFormat`
- Поддержка мультидобавления в корзину
- Автоматическое списание средств при покупке
- Валидация фильтров на клиенте и сервере
- Оптимизация изображений через Nuxt Image (кроме SVG, которые загружаются напрямую)

## 🔍 Тестирование

Проект включает unit-тесты для основных сторов:

```bash
npm test
```

## 🔒 Безопасность и Best Practices

Проект следует современным best practices для веб-безопасности и производительности:

### Security Headers

Приложение использует комплексные заголовки безопасности:

- **X-Content-Type-Options: nosniff** — предотвращает MIME type sniffing
- **X-XSS-Protection: 1; mode=block** — защита от XSS атак
- **X-Frame-Options: SAMEORIGIN** — предотвращает clickjacking
- **Referrer-Policy: strict-origin-when-cross-origin** — контролирует передачу referrer информации
- **Permissions-Policy** — отключает ненужные браузерные API (geolocation, microphone, camera)
- **Content Security Policy (CSP)** — строгая политика безопасности для ресурсов
- **Upgrade-Insecure-Requests** — автоматическое обновление HTTP до HTTPS

### Предотвращение Third-Party Cookies

- **Проксирование изображений**: Все внешние изображения (с `cdn.starpets.pw`) проксируются через IPX на нашем сервере, что предотвращает установку third-party cookies браузером
- **Cross-Origin-Resource-Policy**: Правильная настройка для внешних ресурсов
- **CORS заголовки**: Настроены для API endpoints без передачи cookies

### Оптимизация изображений

- Изображения автоматически конвертируются в современные форматы (WebP, AVIF)
- Проксирование через IPX для оптимизации и предотвращения third-party cookies
- Lazy loading для улучшения производительности

### Кеширование

- Статические ресурсы кешируются на 1 год (`/_nuxt/**`, `/assets/**`)
- Изображения кешируются на 1 день (`/_ipx/**`)
- Агрессивное кеширование данных через TanStack Query (5 минут staleTime, 30 минут gcTime)

### SEO и Meta Tags

- Правильные meta теги для поисковых систем
- Open Graph теги для социальных сетей
- Theme color для мобильных устройств
- Правильная настройка viewport

### Production vs Development

- В production режиме CSP более строгий (без `unsafe-inline` и `unsafe-eval` для скриптов)
- В development режиме разрешены необходимые директивы для Nuxt devtools и HMR

## 📄 Лицензия

Проект создан в рамках тестового задания.
